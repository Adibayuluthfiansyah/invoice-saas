version: "3.8"

services:
  app:
    image: adibayuluthfiansyah/invoice-saas:latest
    ports:
      - "3000:3000"
    env_file:
      - .env
    environment:
      - NODE_ENV=production
      - PORT=3000
      - HOSTNAME=0.0.0.0
    restart: unless-stopped
